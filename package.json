{
  "name": "lucadmin-website",
  "version": "1.0.0",
  "main": "src/index.html",
  "dependencies": {
    "chart.js": "^3.0.1",
    "cropperjs": "^1.5.11",
    "jspdf": "^2.3.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "shx": "^0.3.2"
  },
  "scripts": {
    "dev": "parcel ./src/*/*.html ./src/*.html --public-url ./",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build ./src/*/*.html ./src/*.html --public-url ./",
    "push-gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  }
}
